name: Android APK Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-apk:
    name: Build release APK
    runs-on: ubuntu-22.04
    timeout-minutes: 60
    steps:
      - name: Hello
        run: echo "Build triggered"
